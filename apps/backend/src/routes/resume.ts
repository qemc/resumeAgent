
import type { FastifyInstance } from 'fastify';

export async function resumeRoutes(fastify: FastifyInstance) {
    // Backend implementation removed as per user request
    fastify.log.info('Resume routes placeholder');
}
